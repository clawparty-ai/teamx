# Cross-compilation linker for the Windows GNU target (macOS/Linux host).
# Requires: rustup target add x86_64-pc-windows-gnu (or install the std
# manually) + mingw-w64 (brew install mingw-w64; provides x86_64-w64-mingw32-gcc).
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
